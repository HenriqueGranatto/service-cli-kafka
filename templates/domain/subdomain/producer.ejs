const <%= props.subdomain %> = require('./<%= props.subdomain %>')
const KafkaProducer = require('../../kafka/producers/<%= props.subdomain %>')

class Producer
{
    <%- props.methods %>
}

module.exports = Producer