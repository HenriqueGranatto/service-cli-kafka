const <%= props.subdomain %> = require('./<%= props.subdomain %>')
const <%= props.subdomain %>Consumer = require('../../kafka/consumers/<%= props.subdomain %>')

class Consumer
{
    static subscribe()
    {
        <%- props.subscribers %>
    }

    <%- props.methods %>
}

module.exports = Consumer